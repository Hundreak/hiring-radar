import EmployerTalentRoutePage, {type EmployerTalentRouteProps} from '@/components/employer/talent/talent-route-page';

export default async function EmployerCandidatesPage(props: EmployerTalentRouteProps) {
  return EmployerTalentRoutePage(props);
}
